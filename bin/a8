#!/usr/bin/perl
use warnings;use strict;use utf8;use v5.22;use Oct::a8;
my $a8o=Oct::a8->new(@ARGV);o8($a8o->a8c()); # default new a8 object loads ~/.Hrc but can take a parameter filename for loading && coloring different d8a;
